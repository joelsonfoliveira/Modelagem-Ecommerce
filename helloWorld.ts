const greeting = (person: string) => {
  const msg = "Hello world " + person;
  console.log(msg);
  return msg;
};
const msg = greeting("Abelardo Juvenal");
//document.body.textContent = greeting(msg);
