"use strict";
class Pedido {
    constructor(cliente, produtos, desconto, valorTotal) {
        this.cliente = cliente;
        this.produtos = produtos;
        this.desconto = desconto;
        this.valorTotal = valorTotal;
    }
}
